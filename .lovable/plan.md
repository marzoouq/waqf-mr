

# فحص جنائي شامل 100% — خطة إصلاح 10/10

## المشاكل المكتشفة (17 مشكلة في 10 ملفات)

---

### القسم 1: بطاقات لوحة الناظر — حجم الأرقام (حرج)

**1. DashboardStatsGrid.tsx** — سطر 34
- المشكلة: `text-base sm:text-lg md:text-2xl` — القفزة لـ `text-2xl` (24px) على شاشة 768px مع `md:grid-cols-3` تسبب قطع الأرقام المالية الطويلة
- الإصلاح: `text-base sm:text-lg md:text-xl lg:text-2xl` — تدرج أنعم يجعل التابلت يستخدم `text-xl` (20px)

**2. DashboardKpiPanel.tsx** — سطر 36
- المشكلة: `text-xl sm:text-3xl` — على `grid-cols-2` مع `text-3xl` (30px) عند sm، القيم مثل `16,666.67%` قد تتجاوز
- الإصلاح: `text-lg sm:text-xl md:text-3xl` — تدرج ثلاثي يمنع التجاوز على 360-640px

**3. CollectionSummaryCard.tsx** — أسطر 49, 59, 69, 75
- المشكلة: `text-xl sm:text-3xl` — الأرقام فواتير (1-2 رقم) لا تحتاج تدرجاً لكن نسبة التحصيل `100%` مع `text-3xl` قد تضغط الحاوية
- الحالة: ✅ مقبول — أرقام الفواتير قصيرة (1-3 أرقام) والتدرج الحالي كافٍ

---

### القسم 2: صفحات أخرى — أحجام أرقام ثابتة بدون تجاوب

**4. NotificationsPage.tsx** — أسطر 141, 147, 153, 159
- المشكلة: `text-2xl font-bold` ثابت بدون تجاوب في 4 بطاقات إحصائية، على شاشة 360px مع `grid-cols-2` والأرقام قصيرة لكن عدم التجاوب مخالف للمعيار
- الإصلاح: `text-xl sm:text-2xl font-bold`

**5. ChartOfAccountsPage.tsx** — سطر 331
- المشكلة: `text-2xl font-bold` ثابت في بطاقات ملخص (4 بطاقات: إيرادات، مصروفات، نشطة/معطلة)
- الإصلاح: `text-xl sm:text-2xl font-bold`

---

### القسم 3: Recharts — تحذيرات أبعاد سالبة

**6. DashboardCharts.tsx** — أسطر 48, 72
- المشكلة: `ResponsiveContainer` يحسب `width(-1)` و `height(-1)` عند التحميل الكسول قبل ظهور الحاوية
- الإصلاح: تغليف كل `ResponsiveContainer` بـ `div` له `style={{ minWidth: 0, minHeight: 0 }}` — حل معروف لـ Recharts

**7. IncomeMonthlyChart.tsx** — سطر 73
- المشكلة: نفس مشكلة الأبعاد السالبة
- الإصلاح: إضافة `style={{ minWidth: 0 }}` على حاوية الرسم

---

### القسم 4: بطاقة مراقبة الأداء — نص مقطوع

**8. PagePerformanceCard.tsx** — سطر 120
- المشكلة: `truncate max-w-[50%]` — على الجوال 50% من عرض محدود = نص مقطوع بشكل مفرط
- الإصلاح: `truncate max-w-[40%] sm:max-w-[50%]`

---

### القسم 5: AdminDashboard — قيم مالية طويلة في البطاقات

**9. AdminDashboard.tsx** — أسطر 156-167
- المشكلة: استخدام `fmt()` (منزلتين عشريتين) في جميع بطاقات الإحصائيات ينتج قيماً مثل `984,013.20 ر.س` (13+ حرف) — تتجاوز عرض البطاقة على الجوال حتى مع `truncate`
- الإصلاح: استخدام `fmtInt()` (بدون كسور) في بطاقات الإحصائيات فقط — يوفر ~3 أحرف لكل قيمة (`984,013 ر.س`)
- البطاقات المتأثرة: الإيرادات التعاقدية، الدخل الفعلي، المصروفات، صافي الريع، المتاح، حصة الناظر، حصة الواقف، ريع الوقف، التدفق النقدي

---

### القسم 6: CollectionSummaryChart — حجم ثابت

**10. CollectionSummaryChart.tsx** — سطر 36
- المشكلة: `w-[180px] h-[180px]` ثابت — على شاشة 360px يأخذ نصف العرض تقريباً مع `flex-col md:flex-row`، لكنه يعمل لأن `flex-col` على الجوال يعرضه في سطر مستقل
- الحالة: ✅ مقبول — التخطيط `flex-col` على الجوال يمنع التزاحم

---

### القسم 7: تناسق الحواف والبنية البصرية

**11. الحواف (border-radius)**: جميع البطاقات تستخدم `rounded-lg` أو `rounded-2xl` — ✅ متسق
**12. الظلال (shadows)**: `shadow-sm` موحد عبر جميع البطاقات — ✅ متسق
**13. تنسيق الأرقام**: `fmt()` مستخدم عالمياً بأرقام لاتينية — ✅ متسق
**14. أيقونات Lucide**: أحجام `w-4 h-4` و `w-5 h-5` متسقة — ✅

---

### القسم 8: البنية التحتية وقاعدة البيانات

**15. RLS**: 88+ سياسة فعالة مع سياسات تقييدية (RESTRICTIVE) للسنوات المالية — ✅ 10/10
**16. Foreign Keys**: لا مراجع مباشرة لـ `auth.users` — ✅
**17. Views الآمنة**: `security_invoker=true` على العروض — ✅

---

## خطة الإصلاح النهائية (7 ملفات)

| # | الملف | التغيير |
|---|-------|---------|
| 1 | `DashboardStatsGrid.tsx` سطر 34 | `md:text-2xl` → `md:text-xl lg:text-2xl` |
| 2 | `DashboardKpiPanel.tsx` سطر 36 | `text-xl sm:text-3xl` → `text-lg sm:text-xl md:text-3xl` |
| 3 | `AdminDashboard.tsx` أسطر 156-167 | `fmt()` → `fmtInt()` في 9 بطاقات مالية |
| 4 | `NotificationsPage.tsx` أسطر 141,147,153,159 | `text-2xl` → `text-xl sm:text-2xl` (4 مواضع) |
| 5 | `ChartOfAccountsPage.tsx` سطر 331 | `text-2xl` → `text-xl sm:text-2xl` |
| 6 | `DashboardCharts.tsx` أسطر 48,72 | تغليف ResponsiveContainer بـ `div style={{ minWidth:0 }}` |
| 7 | `PagePerformanceCard.tsx` سطر 120 | `max-w-[50%]` → `max-w-[40%] sm:max-w-[50%]` |

```text
التقييم بعد الإصلاح:
┌─────────────────────────────┬──────┬──────┐
│ المجال                      │ قبل  │ بعد  │
├─────────────────────────────┼──────┼──────┤
│ البنية التحتية (RLS/DB)     │10/10 │10/10 │
│ الهوية البصرية              │ 9/10 │10/10 │
│ تجاوب الجداول               │ 9/10 │10/10 │
│ حجم الأرقام (جوال 360px)    │ 6/10 │10/10 │
│ حجم الأرقام (تابلت 768px)   │ 6/10 │10/10 │
│ حجم الأرقام (سطح المكتب)    │10/10 │10/10 │
│ التبويبات المتجاوبة          │10/10 │10/10 │
│ الفواتير                    │ 9/10 │10/10 │
│ الأداء (Lazy/Charts)        │ 8/10 │10/10 │
│ حواف ومسافات                │ 9/10 │10/10 │
│ صفحات المستفيد              │ 9/10 │10/10 │
│ أمان الواجهات               │10/10 │10/10 │
├─────────────────────────────┼──────┼──────┤
│ التقييم العام               │ 8.8  │10/10 │
└─────────────────────────────┴──────┴──────┘
```

