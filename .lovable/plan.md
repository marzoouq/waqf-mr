

# إصلاح تناقضات تصدير PDF للعقود

## التغييرات المطلوبة — ملف واحد فقط: `src/utils/pdf/entities.ts`

### 1. إضافة دالة `formatDate` لتنسيق التواريخ بالعربي
بدلاً من إرسال التاريخ الخام `2024-01-01`، يُستخدم `toLocaleDateString('ar-SA')` كما في المعاينة.