
# إصلاح خطأ البناء — كاش قديم

## المشكلة

ملف `src/hooks/financial/useAdvanceRequests.test.ts` يحتوي على الاستيرادات الصحيحة (`@/hooks/data/financial/useAdvanceRequests`) في كل الأسطر 50-127. لكن نظام البناء يُبلّغ عن خطأ يشير إلى `./useAdvanceRequests` — مسار غير موجود في الملف فعلياً.

**السبب:** كاش بناء قديم يحتفظ بنسخة سابقة من الملف.

## الإصلاح (خطوة واحدة)

**الملف:** `src/hooks/financial/useAdvanceRequests.test.ts`

إضافة سطر فارغ في نهاية الملف (بعد السطر 131) لتغيير hash الملف وإجبار نظام البناء على إعادة القراءة.

**لا تغييرات وظيفية — تعديل تجميلي فقط.**
