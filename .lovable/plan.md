
نعم، راجعت كل الأسباب المحتملة من الكود الحالي، والمشكلة الفعلية تم تحديدها بدقة:

1) السبب الأساسي (فعلي):
- في `Sidebar.test.tsx` الموك الخاص بـ `useWaqfInfo` غير مطابق لتوقيع الهوك الحقيقي:
  - الحالي: يرجع `{ waqfName: ... }`
  - المطلوب فعليًا من `Sidebar.tsx`: `const { data: waqfInfo } = useWaqfInfo()` ثم `waqfInfo?.waqf_name`
- هذا يعني أن الموك الحالي لا يحتوي `data` ولا `waqf_name`، فيسقط لاستخدام القيمة الافتراضية (`إدارة الوقف`) وقد يفشل التحقق حسب بيئة التشغيل/التوقعات.

2) أسباب ثانوية تزيد احتمالية فشل التحقق:
- `QueryClient` مشترك بين كل الاختبارات (instance واحد global)، وقد يسبب تسرب حالة بين الحالات.
- لا يوجد mock لـ `usePrefetchAccounts`، فاختبارات Sidebar تبقى مرتبطة بسلوك react-query/prefetch مستقبلًا.

خطة الإصلاح (عند التنفيذ):
- تعديل mock `useWaqfInfo` ليطابق البنية الحقيقية:
  - يرجع `{ data: { waqf_name: 'وقف تجريبي', ... }, isLoading: false, error: null }`
- إنشاء `QueryClient` جديد داخل `renderSidebar` لكل اختبار.
- إضافة mock لـ `usePrefetchAccounts` يرجع دالة وهمية (`vi.fn()`).
- تثبيت assertions وفق سياسة الاختبارات بالمشروع:
  - استخدام `getAllByText` عند العناصر المتكررة
  - وتجنب هشاشة التحقق النصي الصارم عندما يلزم.
- إعادة تشغيل مجموعة الاختبارات كاملة للتحقق من زوال فشل التحقق في بيئة CI والمحلي.
