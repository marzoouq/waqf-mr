

# خطة: إضافة بطاقات موبايل لصفحة إدارة المستخدمين

## نتائج التحقق

- **ContractsPage** ✅ — يحتوي بالفعل على عرض بطاقات موبايل (سطور 367-421) مع `md:hidden` و desktop table مع `hidden md:block`. **لا يحتاج أي تعديل.**
- **UserManagementPage** ❌ — يعرض جدولاً فقط مع `overflow-x-auto` بدون بطاقات موبايل.

## التغيير المطلوب: ملف واحد

### `src/pages/dashboard/UserManagementPage.tsx`

إضافة عرض بطاقات موبايل `md:hidden` قبل الجدول وإخفاء الجدول على الجوال `hidden md:block`:

- **البطاقة تعرض**: البريد الإلكتروني، الدور (Badge)، الحالة (مفعل/غير مفعل)، آخر دخول
- **الأزرار**: تفعيل البريد (إن لم يُفعّل)، تعديل، كلمة المرور، حذف (مع إخفاء الحذف للمستخدم الحالي)
- **حالة التحميل**: Skeleton cards بدل نص "جاري التحميل"
- إخفاء الجدول الحالي على الجوال بإضافة `hidden md:block` على wrapper الجدول

