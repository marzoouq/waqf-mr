<div dir="rtl">

# 📋 سجل التغييرات — نظام إدارة وقف مرزوق بن علي الثبيتي

> يوثق التحديثات الرئيسية والإصلاحات الأمنية بترتيب زمني عكسي.

---

## 2026-02-27 — تنظيف وتحسينات أمنية

### 🔒 أمان
- تقييد بيانات المساعد الذكي (AI) حسب دور المستخدم (المستفيد يرى ملخصاً عاماً فقط)
- رفض طلب AI عند فشل جلب الدور بدلاً من افتراض دور خاطئ
- إعادة كتابة `timingSafeEqual` بمقارنة constant-time بدون تسريب الطول
- تعقيم أسماء المستخدمين في إشعارات `admin-manage-users`
- إخفاء تفاصيل العقود عن المستفيدين في تنبيهات انتهاء العقود
- تعقيم رسائل الخطأ في WebAuthn و `generate-invoice-pdf` و `useAccountsPage`
- دمج SELECT+UPDATE في `useUpdateAdvanceStatus` لمنع TOCTOU race condition

### 🧮 مالي
- إصلاح خطأ رياضي في توزيع الحصص (القسمة على 100 بدلاً من مجموع النسب)
- نقل عملية التوزيع إلى دالة مخزنة `execute_distribution` (atomic transaction)
- إصلاح `netRevenue` في صفحة التقارير ليتسق مع `useComputedFinancials`
- إصلاح `sharePercentage: 0` في PDF المستفيد

### 🛠️ تحسينات
- إضافة تحقق وقائي لكشف المستفيدين بدون بريد في صفحة إدارة المستخدمين
- إضافة loading state لعملية التوزيع
- إضافة invalidation عند خطأ التوزيع
- حذف الملفات المهملة (`plan.md`، `FORENSIC-FIX-PLAN.md`، `getInvoiceFileUrl`)

---

## 2026-02-20 — تشديد أمني شامل

- تشديد `log_access_event` — تقييد `anon` لأحداث login/signup فقط
- سحب صلاحيات `EXECUTE` من `anon`/`PUBLIC` للدوال الحساسة
- إزالة `unsafe-eval` من CSP
- تأمين `check-contract-expiry` بمصادقة مزدوجة (service_role + admin JWT)

---

## 2026-02-18 — إطلاق النسخة الأولى

- نظام كامل لإدارة الوقف: عقارات، عقود، إيرادات، مصروفات
- حسابات ختامية مع توزيع تلقائي للحصص
- 4 أدوار: ناظر، محاسب، مستفيد، واقف
- تشفير PII (الهوية الوطنية، الحساب البنكي)
- مساعد ذكي بالعربية (Gemini)
- تقارير PDF وإفصاح سنوي
- نظام إشعارات وتنبيهات تلقائية
- سجل مراجعة شامل (audit log)

</div>
