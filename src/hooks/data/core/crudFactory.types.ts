/**
 * @deprecated نُقل إلى `@/types/data/crudFactory` — هذا الملف يُعيد التصدير للتوافق فقط
 */
export type {
  CrudNotifications,
  TableName,
  Row,
  Insert,
  Update,
  CrudFactoryConfig,
  PaginatedQueryResult,
  CrudQueryOptions,
} from '@/types/data/crudFactory';
