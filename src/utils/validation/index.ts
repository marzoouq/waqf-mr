/**
 * Barrel — utils/validation
 * يُستهلك عبر `@/utils/validation/index` صراحة في عدة ملفات (نمط نادر لكنه موجود في الكود).
 */
export {
  EMAIL_REGEX,
  SAUDI_NATIONAL_ID_REGEX,
  SAUDI_PHONE_REGEX,
  SA_VAT_REGEX,
  IBAN_SA_REGEX,
  DEVICE_SERIAL_REGEX,
} from './regexPatterns';
